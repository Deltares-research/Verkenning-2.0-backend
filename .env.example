# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=True

# Database (if needed later)
# DATABASE_URL=postgresql://user:password@localhost/dbname

# ArcGIS Configuration (if needed)
# ARCGIS_API_KEY=your_api_key
